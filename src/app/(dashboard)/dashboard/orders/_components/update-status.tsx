export default function UpdateStatus({
    order_id,
    table_id,
    status
}:
    {
        order_id: string;
        table_id: string;
        status: string
    }) {
    return (
        <>
        </>
    )
}